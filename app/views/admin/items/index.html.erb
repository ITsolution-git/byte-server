<h1>Listing items</h1>

<table>
  <tr>
    <th>Name</th>
    <th>price</th>
    <th>description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.name %></td>
    <td><%= item.price %></td>
    <td><%= truncate(item.description, :length => 80) %></td>
    <td><%= link_to 'Show', admin_location_item_path(@location,item) %></td>
    <td><%= link_to 'Edit', edit_admin_location_item_path(@location,item) %></td>
    <td><%= link_to 'Destroy', admin_location_item_path(@location,item), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item', new_admin_location_item_path(@location) %>
<%= link_to 'Back', admin_locations_path%>
