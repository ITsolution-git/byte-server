//load modal
if(window.changed_prize){
	$("#modal_holder_prize_level").html("<%= j render partial: "layouts/status_prize",locals: {
    :heading => "Delete",
    :restaurant => @restaurant,
    :status_prize_id => @status_prize_id
    } %>");
	$("#myModal").modal();
}
else {
	
	window.changed_prize = false;
	window.location.href="<%=prize_points_path(@restaurant, :status_prize_id =>@status_prize_id)%>"
}