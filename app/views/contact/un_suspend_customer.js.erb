$("#modal_holder_contact_app").html("<%= j render partial: "layouts/un_suspend_customer",locals: {
    :heading => "",
    :customer_id => @customer_id,
    :restaurant => @restaurant,
    :group_id => @group_id
    } %>");
$("#myModal").modal();